export default class BetOffer {
  constructor(betOffer) {
    const {
      id,
      closed,
      eventId,
      cashIn,
      main,
      startingPrice,
      categoryName,
      cashOutStatus,
    } = event

    return {
      id,
      closed,
      eventId,
      cashIn,
      main,
      startingPrice,
      categoryName,
      cashOutStatus,
    }
  }
}
